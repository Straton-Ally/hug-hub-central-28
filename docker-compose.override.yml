services:
  app:
    # Automatically used by local `docker compose` commands. Coolify invokes
    # docker-compose.yml explicitly, so this host-only mapping is not deployed.
    ports:
      - "${APP_PORT:-8080}:80"
